# Day 1 Status · 2026-05-10

## Completed

- Linux machine verified: Ubuntu 24.04, RTX 5060 Ti 16GB, 31GB RAM.
- SSH from Windows to Linux works.
- Five golden PDFs placed in `~/lit-system/corpus/golden/`.
- Git repository initialized.
- Ollama service installed and running.
- `qwen2.5:14b` runs locally with GPU acceleration.
- `bge-m3` can respond to simple embedding calls.
- `paper-qa` installed in isolated uv environment.
- `use_doc_details=False` and `multimodal=False` verified.

## Current blocker

PaperQA2 local-only smoke test has not produced a successful ANSWER yet.

Main causes:

1. `bge-m3` via Ollama intermittently returns NaN embeddings on technical document text.
2. `qwen2.5:14b` is being truncated to 4096 context tokens under Ollama default settings.

## Diagnosis

This is an Ollama-layer temporary stack problem, not a failure of the whole project design.

## Next action

Run v7 with:

- Embedding: `nomic-embed-text`
- LLM: `qwen2.5-14b-8k`
- PaperQA2 options:
  - `use_doc_details=False`
  - `multimodal=False`
  - `recurse_subdirectories=False`
  - absolute golden corpus path only

## Stop rule

If v7 and v8 both fail, stop debugging PaperQA2 for Week 0 and proceed to Week 1/2 minimal self-built RAG.
PaperQA2 will remain a later benchmark rather than the core system.
